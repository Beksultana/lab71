const initialState = {
    dishes: {
        dishesTitle: '',
        dishesPrice: '',
        dishesImage: '',
    }
};

const reducer = (state = initialState, action) => {

    return state;
};

export default reducer;